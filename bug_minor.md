# Bug & Minor Fix List
- [x] `index.html`: Header link ke Google Fonts (Poppins/Open Sans).
- [ ] Admin: Logic simpan data (saat ini hanya download, perlu backend/storage).
- [ ] PWA: Setup `manifest.json` dan `sw.js` (offline mode) — icon + meta OK; cache list masih minim.
- [x] Galeri: Implementasi lightbox (klik foto → modal besar).
- [x] UI: Tambahkan style aktif di navbar berdasarkan hash saat ini.
- [x] Mobile: touch target 48px, hamburger full-screen, safe-area, filter stack, form modal bottom sheet.
